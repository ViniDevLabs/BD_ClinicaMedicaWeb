info:
  name: Listar médicos
  type: http
  seq: 2

http:
  method: GET
  url: "{{API_URL}}/medicos"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
