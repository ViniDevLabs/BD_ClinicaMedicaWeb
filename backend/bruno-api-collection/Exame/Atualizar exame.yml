info:
  name: Atualizar exame
  type: http
  seq: 5

http:
  method: PUT
  url: "{{API_URL}}/exames/1"
  body:
    type: json
    data: |-
      {
        "idAgendamento": 1,
        "nomeExame": "Hemograma completo",
        "dataSolicitacao": "2026-07-10",
        "localRealizacao": "EXTERNO",
        "observacoesMedicas": "Laboratório parceiro.",
        "arquivoLaudoPath": null,
        "status": "SOLICITADO"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
