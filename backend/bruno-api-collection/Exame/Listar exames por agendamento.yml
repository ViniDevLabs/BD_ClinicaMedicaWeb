info:
  name: Listar exames por agendamento
  type: http
  seq: 4

http:
  method: GET
  url: "{{API_URL}}/exames/agendamento/1"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
