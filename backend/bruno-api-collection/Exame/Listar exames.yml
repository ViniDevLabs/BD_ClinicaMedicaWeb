info:
  name: Listar exames
  type: http
  seq: 2

http:
  method: GET
  url: "{{API_URL}}/exames"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
