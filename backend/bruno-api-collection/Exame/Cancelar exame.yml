info:
  name: Cancelar exame
  type: http
  seq: 6

http:
  method: PATCH
  url: "{{API_URL}}/exames/1/cancelar"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
