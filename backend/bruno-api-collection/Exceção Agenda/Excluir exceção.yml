info:
  name: Excluir exceção
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{API_URL}}/excecoes-agenda/1"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
