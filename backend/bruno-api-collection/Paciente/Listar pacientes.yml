info:
  name: Listar pacientes
  type: http
  seq: 2

http:
  method: GET
  url: "{{API_URL}}/pacientes"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
