info:
  name: Listar horários disponíveis
  type: http
  seq: 8

http:
  method: GET
  url: "{{API_URL}}/agendamentos/horarios-disponiveis?idMedico=2&data=2026-07-10"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
