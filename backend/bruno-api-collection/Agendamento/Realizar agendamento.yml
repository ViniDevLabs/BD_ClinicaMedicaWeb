info:
  name: Realizar agendamento
  type: http
  seq: 11

http:
  method: PATCH
  url: "{{API_URL}}/agendamentos/1/realizar"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
