info:
  name: Listar agendamentos por paciente
  type: http
  seq: 5

http:
  method: GET
  url: "{{API_URL}}/agendamentos/paciente/3"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
