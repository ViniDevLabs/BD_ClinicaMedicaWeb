info:
  name: Confirmar agendamento
  type: http
  seq: 10

http:
  method: PATCH
  url: "{{API_URL}}/agendamentos/1/confirmar"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
