info:
  name: Excluir agendamento
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{API_URL}}/agendamentos/1"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
