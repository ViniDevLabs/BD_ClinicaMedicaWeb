info:
  name: Listar agendamentos
  type: http
  seq: 2

http:
  method: GET
  url: "{{API_URL}}/agendamentos"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
