info:
  name: Listar disponibilidade por ID
  type: http
  seq: 3

http:
  method: GET
  url: "{{API_URL}}/disponibilidades-padrao/1"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
