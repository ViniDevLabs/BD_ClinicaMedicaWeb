info:
  name: Listar disponibilidades por médico
  type: http
  seq: 4

http:
  method: GET
  url: "{{API_URL}}/disponibilidades-padrao/medico/2"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
