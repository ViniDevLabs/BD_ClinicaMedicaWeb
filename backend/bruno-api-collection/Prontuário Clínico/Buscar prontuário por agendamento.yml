info:
  name: Buscar prontuário por agendamento
  type: http
  seq: 4

http:
  method: GET
  url: "{{API_URL}}/prontuarios/agendamento/1"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
