info:
  name: Excluir prontuário
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{API_URL}}/prontuarios/1"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
